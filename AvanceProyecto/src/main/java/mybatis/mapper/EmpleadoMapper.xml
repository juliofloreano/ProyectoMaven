<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.EmpleadoMapper">

	<select id="login" parameterType="java.util.Map" resultType="model.Empleado">
		select * from Empleado where usuario = #{user} and contraseña = #{password}
	</select>
</mapper>
